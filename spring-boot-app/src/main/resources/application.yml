server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

asoc:
  admin-token: changeme-admin-token
  orchestrator-base-url: http://localhost:8000
  default-block-ttl-seconds: 900

